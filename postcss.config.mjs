// Tailwind CSS v4's PostCSS integration — the whole plugin, no separate
// `autoprefixer`/`postcss-import` entries needed (v4 bundles both).
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
