# Copy this file to ".env" inside gigradar's XDG data directory (NOT the
# repo root — see docs/ARCHITECTURE.md), fill in real values, and
# `chmod 600` it. Never commit a real .env file.
#
# One placeholder line per "env:VAR_NAME" reference used in
# config.example.json's sources[].settings.
BRAINTRUST_API_KEY=example-placeholder-not-a-real-key-00000000
