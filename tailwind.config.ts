// Tailwind CSS v4 auto-detects template files and no longer requires a
// `content` glob to be listed here — this file exists for future
// customization (theme tokens, plugins) rather than as required wiring.
// See src/app/globals.css for the `@import "tailwindcss"` entry point.
import type { Config } from "tailwindcss";

const config: Config = {};

export default config;
